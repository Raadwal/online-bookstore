const config = {};

// ========== Host ==========
config.host = {
  hostname: "localhost",
  port: "8000",
};

// ========== Database ==========
config.database = {
  dbHost: "localhost",
  dbPort: "5432",
  dbUser: "admin",
  dbPass: "admin",
  dbName: "ksiegarnia",
};

module.exports = config;
